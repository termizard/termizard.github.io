services:
  build-static:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: termizard-build
    volumes:
      - ./:/result
    command: ["sh", "-c", "cp -r /out/* /result"]
